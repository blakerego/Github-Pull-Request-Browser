query {
  organization(login: "lodash") {
    name
    repositories(first: 10) {
      edges {
        node {
          pullRequests(first:50,states:MERGED) {
            edges {
              node {
                mergedAt
                title
                url
                baseRefName
                headRefName
                repository {
                  name
                }
                author {
                  avatarUrl
                  login
                  url
                }
              }
            }
          }
        }
      }
    }
  }
}
